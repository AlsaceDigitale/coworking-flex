parameters:
    git_dir: .
    bin_dir: vendor/bin
    tasks:
        phpcs:
            standard: PSR2
            ignore_patterns: [./tests/*, ./src/Migrations/*, ./src/Kernel.php, ./public/*, ./config/*, ./vendor/*, ./var/*, ./translations/*]